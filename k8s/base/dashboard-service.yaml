
# Churn Dashboard — Service

apiVersion: v1
kind: Service
metadata:
  name: churn-dashboard-service
  namespace: telco-churn
  labels:
    app: churn-dashboard
    component: dashboard
spec:
  type: ClusterIP
  ports:
    - port: 8501
      targetPort: 8501
      protocol: TCP
      name: http
  selector:
    app: churn-dashboard
