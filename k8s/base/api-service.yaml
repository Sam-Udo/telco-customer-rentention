
# Churn API — Service (ClusterIP for internal, LoadBalancer for external)

apiVersion: v1
kind: Service
metadata:
  name: churn-api-service
  namespace: telco-churn
  labels:
    app: churn-api
    component: api
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: churn-api
